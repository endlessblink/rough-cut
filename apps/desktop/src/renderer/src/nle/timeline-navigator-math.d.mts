export function navigatorFrameAtClientX(clientX: number, surfaceLeft: number, surfaceWidth: number, durationFrames: number): number;
export function navigatorClipStyle(timelineIn: number, timelineOut: number, durationFrames: number): { left: string; width: string } | null;
